# Install runtime dependencies only, using npm's modern flag.
#
# This replaces the deprecated `production=true` / `--production` config that
# makes npm print:
#   npm warn config production Use `--omit=dev` instead.
# This project has no devDependencies, so omitting dev deps is always correct.
omit=dev
